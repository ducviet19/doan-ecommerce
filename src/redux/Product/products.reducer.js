import productsTypes from "./products.types"

const INITIAL_STATE = {
    products: [],
    product: {}
}

const productReducer = (state = INITIAL_STATE , action) => {
    switch(action.type) {
        
    }
}